# Frontend

React + TypeScript frontend for the Smart Urban and Crime Intelligence System.

## Stack
- React
- TypeScript
- Leaflet
- OpenStreetMap tiles

## Local setup
1. Install dependencies with `npm install`.
2. Copy `.env.example` to `.env` if you need to configure the API URL.
3. Start the app with `npm run dev`.
